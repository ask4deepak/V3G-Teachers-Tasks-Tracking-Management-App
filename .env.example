# Application Environment Configuration
PORT=3000
NODE_ENV=development
APP_BASE_URL=http://localhost:3000

# PostgreSQL Connection (Railway or local PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/teachers_tasks_db

# Session Secret Key
SESSION_SECRET=super-secret-session-key-v3g-teacher-task-tracking

# Email SMTP Settings (Optional in dev, mock mode fallback enabled)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=tasks@institution.edu
